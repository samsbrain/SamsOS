# SamOS Finance

The monthly finance meeting is generated from `Config/master.yaml` and appears
in the calendar and dashboard. Add prior `.xlsx`, `.xls`, `.csv`, or `.tsv`
worksheets to this folder when they are available. The dashboard will list the
files automatically so each meeting can open the supporting history from one
place.
