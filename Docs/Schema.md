# SamOS Schema

## Purpose

This document defines every object that exists inside SamOS.

SamOS is built around objects rather than calendar events.

Each object contains structured data that can be compiled into multiple outputs including:

- Apple Calendar
- Apple Reminders
- Dashboard
- Knowledge Base
- Weekly Briefings
- Future integrations

This schema is the source language of SamOS.
